---
name: Git workflow preference
description: User prefers committing and pushing directly to master, not creating branches or PRs
type: feedback
---

Push directly to master. Do not create feature branches or PRs unless explicitly asked.

**Why:** User finds the branch/PR workflow unnecessary overhead for this project.

**How to apply:** When the user asks to commit and push, stage files, commit, and `git push` to master directly.
